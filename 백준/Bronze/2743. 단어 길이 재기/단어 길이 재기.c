#include <stdio.h>
#include <string.h>
#include <limits.h>
#include <math.h>


int main() {

    char a[101];
    scanf("%s", &a);
    int len;
    len = strlen(a);
    printf("%d", len);

    return 0;
}
